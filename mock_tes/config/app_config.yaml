# General server settings
server:
    host: 0.0.0.0
    port: 9001
    debug: True

# OpenAPI specs
openapi:
    tes: mock_tes/specs/schema.task_execution_service.d55bf88.openapi.modified.yaml
    cost_update: mock_tes/specs/schema.task_execution_service.config_update.openapi.yaml

# TES service settings
task_info:
    currency: ARBITRARY
    time_unit: SECONDS
    unit_costs:  # per min
        cpu_usage: 1  # per core
        memory_consumption: 1  # per GB
        data_storage: 1  # per GB
        data_transfer: 0.01  # per GB and 1000 km
